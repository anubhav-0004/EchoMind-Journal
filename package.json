{
  "name": "echomind",
  "private": true,
  "packageManager": "npm@11.5.2",
   "engines": {
    "node": "20.x"
  },
  "workspaces": [
    "apps/web",
    "apps/server",
    "apps/mobile",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo run dev",
    "build": "turbo run build",
    "db:migrate": "turbo run db:migrate"
  },
  "devDependencies": {
    "turbo": "latest",
    "typescript": "^5.0.0"
  }
}